# 数据模型

## 概述
本项目不使用数据库，数据以文件形式组织，输入与输出均为静态文件。

## 数据文件

### 规则列表
- **位置:** `ruleset/`
- **格式:** 一行一条规则（文本）。

### 代理样例
- **位置:** `yaml_generator/fake_proxies.json`
- **用途:** 作为生成器的基础配置输入。

### 生成产物
- **位置:** `yamls/`
- **格式:** YAML 覆写配置（自动生成）。
