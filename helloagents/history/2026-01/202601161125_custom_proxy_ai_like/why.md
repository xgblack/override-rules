# 变更提案: 自定义出国分组参考 AI 实现

## 需求背景
当前“自定义出国”分组的可选代理列表使用所有现有分组聚合。需要改为参考 AI 分组的实现逻辑，生成与 AI 类似的可选项，但不直接引用 AI 分组。

## 变更内容
1. 调整“自定义出国”分组的代理列表构建逻辑，参考 AI 分组的组成方式。
2. 若无法构建 AI 风格列表，则回退到现有兜底逻辑。

## 影响范围
- **模块:** convert（后置覆写脚本）
- **文件:** post_convert.js
- **API:** 无
- **数据:** 无

## 核心场景

### 需求: AI 逻辑参考
**模块:** convert
“自定义出国”分组的可选项采用 AI 分组的组合规则，而非直接引用 AI 分组。

#### 场景: 兜底回退
当无法识别 AI 逻辑所需分组时，回退为当前兜底列表。

## 风险评估
- **风险:** 分组命名变化导致 AI 逻辑无法完全匹配。
- **缓解:** 保留兜底逻辑并使用名称匹配回退。
